class Solution:
    def rob(self, nums: List[int]) -> int:
        ans=[0]*len(nums)
        ans[0]=nums[0]
        ans[1]=max (nums[0],numd[1])
        for i in range (2,len(nums)):
            ans[i]=max(ans[i-1],nums[i]+ans[i-2])
        return ans[len(nums)-1]
         
         

        
